
# Python中的变量不需要声明。每个变量在使用之前都必须赋值，变量赋值以后该变量才会被创建
# 变量就是变量，它没有类型 我们所说的类型是变量所指的内存中对象的类型
# 等号（）

a = 5
# a + 1 不会改变原来的值
print(a + 1)
print(a)


doubledb = 1024.88
# 不会改变原来的值
print(int(doubledb))
print(doubledb)